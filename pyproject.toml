[project]
name = "quant-stock-tool"
version = "0.1.0"
description = "A small, extensible stock quant research and backtesting toolkit."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
quanttool = "quanttool.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
